# ════════════════════════════════════════════════════════════════
# Frontend Alumco - Configuración por Contexto
# NUNCA versiones .env con valores reales — git ignore automático
# ════════════════════════════════════════════════════════════════

# ── URL Base del Backend API ──────────────────────────────────────
# Local: http://localhost:3000 (backend local o remoto)
# Nube: https://api.tudominio.com (backend en producción)
# 
# Nota: Opcional — si no se define, fallback automático a http://localhost:3000
VITE_API_URL=http://localhost:3000
